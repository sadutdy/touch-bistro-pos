/** SQLite schema and query helpers live in db.js. This module exposes the domain names for integration. */
module.exports = { Tables: 'restaurant_tables', MenuItem: 'menu_items', Order: 'orders', Transaction: 'transactions', Inventory: 'inventory' };
