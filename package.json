{
  "name": "touch-bistro-pos",
  "version": "1.0.0",
  "private": true,
  "description": "Touch-optimized restaurant POS with Express, SQLite and Socket.io",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client:dev": "npm --prefix client run dev",
    "client:build": "npm --prefix client run build",
    "build": "npm --prefix client run build"
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "morgan": "^1.10.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": { "nodemon": "^3.1.9" }
}
